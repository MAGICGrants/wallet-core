{
  "note": "Generated by packages/wallet_monero/tool/make_fixtures.dart. Every seed here is published; no fixture holds mainnet value.",
  "networkType": 2,
  "password": "fixture-password",
  "fixtures": {
    "skylight_plaintext_prefs": {
      "prefs": "prefs.json",
      "keys": [
        "connectionAddress",
        "connectionType",
        "walletRestoreHeight",
        "unusedSubaddressIndex",
        "serverSupportsSubaddresses"
      ],
      "expects": "these read back through the wallet layer with no migration step"
    },
    "corrupt_truncated": {
      "cacheFile": "xmr_cache",
      "expects": "WalletCacheStore.load returns an empty map"
    },
    "corrupt_bad_gcm_tag": {
      "cacheFile": "xmr_cache",
      "expects": "WalletCacheStore.load returns an empty map, and does not throw"
    }
  }
}
